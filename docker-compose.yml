version: '3.8'

services:
  demo-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data  # Persist SQLite data if needed
    restart: unless-stopped
